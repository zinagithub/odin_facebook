<%= form_for(current_user.likes.find_by(post_id: post.id), html: { method: :delete },remote: true) do |f| %>
	<%= hidden_field_tag :post_id, post.id %>
	<%= f.button fa_icon("heart"), class: "btn my-btn my-btn-unlike", value: 'unlike' %>
<% end %>